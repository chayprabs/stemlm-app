node_modules/
.output/
.wxt/
stats.html
*.log
.env
.env.local
.DS_Store
dist/

# Local evaluation inputs/outputs — never commit question sets, captured answers,
# or generated eval reports. Keep the QA corpus external to the repo.
artifacts/
eval-out/
output/

# Blind-elicitation samples reference a private local corpus by absolute path.
# Kept on disk for re-measurement, never published.
docs/history/blind-*.json
