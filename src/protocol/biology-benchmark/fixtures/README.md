Cached Gemini responses for biology benchmark specs.

Each file should be named `qNN.stemlm` and contain raw capsule text from the live AI path (protocol + Biology playbook). Generate with:

```bash
GEMINI_API_KEY=... pnpm fixtures:biology
```
